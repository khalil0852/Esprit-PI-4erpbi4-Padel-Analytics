global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'padel-app'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['padel-app:5000']

rule_files:
  - '/etc/prometheus/alerts.yml'